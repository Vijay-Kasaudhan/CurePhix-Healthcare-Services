declare class SMSService {
    private snsClient;
    private logger;
    constructor(awsRegion?: string);
    sendSMS(phoneNumber: string, messageBody: string): Promise<string>;
}
export { SMSService };
